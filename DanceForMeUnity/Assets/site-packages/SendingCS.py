import socket

def sendingData(data):
    host, port = "127.0.0.1", 25001
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.connect((host,port))
    testing = data

    sock.sendall(testing.encode("UTF-8")) #Send Data to C#

